variables:
  CI_DEBUG_TRACE: "false"

# workflow:
#   rules:
#     - if: '$CI_PIPELINE_SOURCE == "trigger"'

include:
  - local: include.yml

stages:
  - pre-rocket
  - build
  - deploy
  - post-deploy

before_script:
    - apt-get update
    - apt-get install -y ansible docker.io wget unzip

    # Install Terraform
    - wget https://releases.hashicorp.com/terraform/1.6.6/terraform_1.6.6_linux_amd64.zip
    - unzip terraform_1.6.6_linux_amd64.zip
    - mv terraform /usr/local/bin/

    - terraform version
    - ansible --version
    - python3 --version

pre-rocket:
  stage: pre-rocket
  before_script:
    - apt-get update
    - apt-get install -y ansible
  script:
    - echo "Started pre-rocket stage for $Enviornment for $Tenant"
    - ansible-playbook pre-rocket/main.yml

build:
  stage: build
  script:
    - echo "Started build stage for $Enviornment for $Tenant"
    - ansible-playbook build/main.yml

deploy:
  stage: deploy
  script:
    - echo "Deploying to $Enviornment for $Tenant"
    - echo "Started build stage for $Enviornment for $Tenant"
    - ansible-playbook deploy/main.yml

post-deploy:
  stage: post-deploy
  script:
    - echo "Completed for $Tenant"
    - echo "Started build stage for $Enviornment for $Tenant"
    - ansible-playbook post-deploy/main.yml